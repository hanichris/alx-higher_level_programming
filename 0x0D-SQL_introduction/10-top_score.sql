-- List all the records in the table `second_table`.
-- The records are displayed in the order of score and name.
-- The records are ordered by the score.
SELECT score, name FROM second_table ORDER BY score DESC;
